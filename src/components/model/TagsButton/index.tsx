import { TagsButtonLoadingPresentation } from './presentations/loading';

type Props = object;

export const TagsButton: React.FC<Props> = () => (
  <>
    this is TagsButton component
    <TagsButtonLoadingPresentation />
  </>
);
